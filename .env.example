# Environment Configuration Template
# Copy this file to .env and update with your actual values

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
# For local MongoDB:
# MONGODB_URI=mongodb://localhost:27017/prodiny

# For MongoDB Atlas (replace with your actual connection string):
MONGODB_URI=mongodb+srv://your_username:your_password@your_cluster.mongodb.net/prodiny?retryWrites=true&w=majority&appName=YourApp

# JWT Configuration (use a long, secure secret for production)
JWT_SECRET=your_super_secure_jwt_secret_key_at_least_32_characters_long_change_this
JWT_EXPIRE=7d

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual MongoDB credentials
# 3. Generate a secure JWT secret for production use
# 4. Never commit the actual .env file to version control
